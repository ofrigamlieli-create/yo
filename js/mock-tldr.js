window.KaniMockTldr = {
  short: "This passage explains how machine learning models are trained on large datasets to recognize patterns, enabling them to make predictions on new, unseen data.",

  bullets: [
    "Machine learning models learn by processing large amounts of labeled training data.",
    "During training, the model adjusts internal parameters to minimize prediction errors.",
    "Once trained, the model can generalize its learning to new, unseen inputs.",
    "The quality of training data directly affects the accuracy of the model's predictions.",
    "Overfitting occurs when a model memorizes training data rather than learning generalizable patterns."
  ],

  simple: "Imagine teaching a child to recognize dogs by showing them thousands of dog photos. Machine learning works the same way — a computer looks at huge amounts of examples, learns the patterns, and then uses those patterns to make guesses about new things it hasn't seen before."
};
